DATABASE_URL=postgres://localhost/bookkeeper_agent_development
SECRET_KEY_BASE=replace-me
GEMINI_API_KEY=replace-me
# Development only: when GEMINI_API_KEY is blank, the API uses a stub extractor so uploads
# still flow through the pipeline. Set DISABLE_STUB_AI=1 to require a real key locally.
# DISABLE_STUB_AI=1
ACTIVE_STORAGE_SERVICE=local
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_S3_BUCKET=
